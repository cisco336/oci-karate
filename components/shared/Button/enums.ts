export enum buttonColor {
    Primary = 'primary',
    Secondary = 'secondary',
    Info = 'info',
    Success = 'success',
    Warning = 'warning',
    Danger = 'danger',
    Text = 'text',
    Accent = 'accent',
}

export enum buttonVariants {
    Solid = 'solid',
    Outline = 'outline',
    Ghost = 'ghost',
    Link = 'link',
}

export enum buttonTypes {
    Button = 'button',
    Submit = 'submit',
    Reset = 'reset',
}
